import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:portfolio/app/modules/projects/projects_controller.dart';
import 'package:portfolio/app/modules/projects/projects_module.dart';

void main() {
  initModule(ProjectsModule());
  // ProjectsController projects;
  //
  setUp(() {
    //     projects = ProjectsModule.to.get<ProjectsController>();
  });

  group('ProjectsController Test', () {
    //   test("First Test", () {
    //     expect(projects, isInstanceOf<ProjectsController>());
    //   });

    //   test("Set Value", () {
    //     expect(projects.value, equals(0));
    //     projects.increment();
    //     expect(projects.value, equals(1));
    //   });
  });
}
