import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:portfolio/app/modules/contact/contact_page.dart';

main() {
  testWidgets('ContactPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContactPage(title: 'Contact')));
    //  final titleFinder = find.text('Contact');
    //  expect(titleFinder, findsOneWidget);
  });
}
